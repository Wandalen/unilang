[package]
name = "dummy_lib"
version = "0.1.0"
edition = "2021"

[workspace]
# Empty workspace to avoid conflicts with parent workspace

[lib]
crate-type = ["cdylib"]

[dependencies]
unilang = { path = "../../.." }