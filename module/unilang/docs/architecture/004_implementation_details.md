# Architecture: Implementation Details

### Scope

- **Purpose:** Document internal implementation details of the PHF optimization, build system, and codegen
- **Responsibility:** Explains how the static registry is generated and how PHF achieves zero-overhead lookup
- **In Scope:** PHF internals, build script architecture, codegen design, performance analysis
- **Out of Scope:** Public API contracts, feature requirements, vision/scope

### Compile-Time Optimization Strategy

#### Perfect Hash Functions (PHF)

The `StaticCommandMap` wrapper uses Perfect Hash Functions (PHF) internally to achieve zero-overhead command lookups. This is an implementation detail that must remain hidden from the public API.

**Why PHF:**
- O(1) guaranteed lookup time (not average case)
- Zero runtime memory allocation
- Generated at compile-time via `build.rs`
- No hash computation at runtime
- Typically 10-50x faster than `HashMap` for static data

**Implementation Pattern:**

The build script generates an internal PHF constant containing all static command definitions, wrapped in the public `StaticCommandMap` type. The internal PHF map is never exposed to downstream crates — only the opaque wrapper is visible. Generated constant names end with `_INTERNAL` to signal implementation details.

**Critical Requirements:**
1. Never expose PHF types in public signatures — always wrap in `StaticCommandMap`
2. Mark the PHF constructor as `#[doc(hidden)]` — only build.rs should use it
3. All wrapper methods must be `#[inline]` — ensure zero-cost abstraction
4. Generated constant names end with `_INTERNAL` — signals implementation detail

**Dependencies:**
- `phf` — Build dependency only (not required by downstream crates)
- `phf_codegen` — Build script code generation

### Performance Characteristics

#### Static Registry (PHF-based)

- Startup overhead: ~5μs (map initialization)
- Lookup latency: ~50-200ns (P99 < 100ns in optimized builds)
- Memory overhead: Zero runtime allocation
- Throughput: >10M lookups/second

#### Dynamic Registry (HashMap-based)

- Startup overhead: ~10-100μs (depends on command count)
- Lookup latency: ~500-5000ns (P99 < 1μs)
- Memory overhead: ~48 bytes per command + allocation overhead
- Throughput: ~1M lookups/second

#### Why 10-50x Performance Difference

1. PHF has no hash computation (precomputed at build time)
2. PHF has perfect collision-free lookups (guaranteed O(1))
3. PHF data is in read-only memory (better cache locality)
4. No allocator involvement (zero malloc/free overhead)

### Build System Integration

The `build.rs` script generates static registries by parsing YAML manifests, generating PHF map source via `MultiYamlAggregator`, writing the output to `$OUT_DIR` as `static_commands.rs`, and the application includes it at compile time. This ensures compile-time validation of all command definitions, zero runtime parsing overhead, type-safe static command access, and no YAML parser dependency in the production binary.

### Maintainer Notes

When updating this implementation, ensure that:
1. Public API never exposes PHF types
2. All examples use domain terms (not "PHF map")
3. User documentation focuses on capabilities, not implementation
4. Deprecation warnings guide users toward static registration

### Analyses

| File | Relationship |
|------|--------------|
| [001_api_analysis.md](../analysis/001_api_analysis.md) | Analysis of static registry and PHF performance patterns |

### Features

| File | Relationship |
|------|--------------|
| [001_command_registry.md](../feature/001_command_registry.md) | FR-REG-9 build-time validation implemented here |

### Architectures

| File | Relationship |
|------|--------------|
| [001_mandates.md](../architecture/001_mandates.md) | Mandates that require this PHF implementation |

### Invariants

| File | Relationship |
|------|--------------|
| [002_non_functional_requirements.md](../invariant/002_non_functional_requirements.md) | NFR-PERF-1 achieved by this implementation |
| [006_build_runtime_separation.md](../invariant/006_build_runtime_separation.md) | Compile-time-only guarantee this implementation enforces |

### APIs

| File | Relationship |
|------|--------------|
| [001_public_types.md](../api/001_public_types.md) | Public types generated by this system |

### Sources

| File | Relationship |
|------|--------------|
| `build/main.rs` | Build script orchestration |
| `build/codegen.rs` | PHF code generation |
| `build/discovery.rs` | YAML/JSON file discovery |
| `build/type_hints.rs` | Type hint generation |

### Tests

| File | Relationship |
|------|--------------|
| `tests/build/` | Build pipeline validation |
